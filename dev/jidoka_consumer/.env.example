# The consumer reads the repo-level ../../.env first, then this app's .env.
# Put local overrides in dev/jidoka_consumer/.env.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: stable local secret for Jido.AI ReAct token signing.
# REACT_TOKEN_SECRET=replace-with-a-long-random-string
